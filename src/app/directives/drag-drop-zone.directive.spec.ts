import { DragDropZoneDirective } from './drag-drop-zone.directive';

describe('DragDropZoneDirective', () => {
  it('should create an instance', () => {
    const directive = new DragDropZoneDirective();
    expect(directive).toBeTruthy();
  });
});
